# Optional mobile overrides. Normal local start needs no .env file.
EXPO_PUBLIC_GATEWAY_URL=https://api.neijiecave.com
EXPO_PUBLIC_ASSISTANT_MODE=live
# Use start:journal-preview only for explicit local simulation.
# Server secrets belong in Cloudflare Worker Secrets or apps/gateway/.dev.vars.
# See docs/operations/ai-service.md. Never put provider keys in Expo variables.
