# 2026-09-09 试聊用量

本次开发测试共 5 次请求，5 次均收到模型 usage，HTTP 均为 200。模型为 deepseek-v4-flash；模型回复时间为 03:02:20–03:04:06 UTC。未保存聊天正文。

| 项目 | Token |
| --- | ---: |
| 输入总量 | 3164 |
| 输入缓存命中 | 2560 |
| 输入缓存未命中 | 604 |
| 输出总量 | 1247 |
| 输入与输出合计 | 4411 |

按 [DeepSeek 官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/) 在本次调用时段（周三高峰）的人民币单价，每百万 token 缓存命中 0.10 元、未命中 3.0 元、输出 9.0 元：

`(2560 × 0.10 + 604 × 3.0 + 1247 × 9.0) / 1000000 = 0.013291 元`

约 1.33 分，每次平均 0.0026582 元。这是基于模型返回 token 与公开价格的推算，不是账户扣费账单，不含 Cloudflare 等基础设施成本。

用户要求按本次用量设小时额度、每日为五倍。沿用当前产品的调用次数口径，配置为每账号每小时 5 次、每天 25 次；并非 4411 token 的硬上限。同样次数的成本会随上下文长度、输出和缓存命中率变化。按本样本均值，25 次约 0.066455 元，仅作估算，不是每日费用保证。

窗口保持 UTC 整点、零点重置，客户端显示当地时间。保留已使用计数；本次测试会计入当前窗口。配额在服务端按登录账号执行，跨设备共享。
